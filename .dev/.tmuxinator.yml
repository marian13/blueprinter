##
# NOTE: Tmuxinator config options.
# - https://github.com/tmuxinator/tmuxinator#create-a-project
#
name: blueprinter
root: .

on_project_start:
  - task editor:open
  - task docker:start
  - task github:open

windows:
  - main:
      layout: even-vertical
      root: .
      panes:
        - RAILS_PORT=8400 task docker:server:open
        - git status
